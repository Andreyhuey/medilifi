import { createApi, fetchBaseQuery } from "@reduxjs/toolkit/query/react";

const baseUrl = "https://";

const createRequest = (url:any) = { url });

export const patientDataApi = createApi({})

export const {

} = patientDataApi;
